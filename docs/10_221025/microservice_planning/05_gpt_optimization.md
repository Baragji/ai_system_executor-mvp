Got it—RS_Summary_qodo_original.md is the **single source of truth**. Here’s the plain-English game plan you can hand to your other AI assistant, broken into small, safe passes.

# What the RS document is (non-technical)

Think of it as a **before-photos + moving plan** for a house renovation:

* **Where things are now:** exact file/line locations in the monolith.
* **What to move next:** which pieces belong to each future service.
* **How to move safely:** a phased order with rollback so nothing “explodes”.

This is the same spirit as industry-standard “**Strangler Fig**” (migrate piece by piece, keep the old path alive) and “**Branch-by-Abstraction**” (introduce a seam/flag, switch behind it), both designed to reduce risk by going incremental. ([martinfowler.com][1])

# How to use it with a context-limited AI

Run it **one phase per session**. Each session is tiny: verify → move one slice → verify again. That mirrors best-practice refactors: small steps, constant green tests, easy rollbacks. ([claydowling.com][2])

Two hand-off styles (both safe):

**Option A (recommended):**
Send **only the current phase** from RS_Summary_qodo_original.md + the prompt below (“Phase Starter”). The assistant does Phase 1, shows evidence, stops. You review. Then you paste Phase 2, and so on.

**Option B:**
Send the whole “Extraction Plan (Phases)” section, **but tell the AI** to do **Phase 1 only**, stop for review, then proceed phase-by-phase.

# Why this is safe

* You keep a **feature flag** (e.g., `SERVICES_SPLIT`): flip it off → instant rollback to the monolith path. That is classic **feature-toggle** safety. ([martinfowler.com][3])
* You follow the **Strangler Fig** style: route a small flow to the new service, prove it works, expand gradually. ([martinfowler.com][1])

# Evidence-first verification (every session)

Tell the AI to **paste the full outputs** (exit code 0) before and after the change:

```
npm run lint
npm run typecheck
npm test
npm run build
```

(If you have contract/SBOM/provenance steps in your repo, include those too.) This “gates” pattern (green → change → green) is exactly what current agentic coding tools and research recommend: **human-in-the-loop checks, incremental diffs, tests as guardrails**. ([anthropic.com][4])

# 7 tiny sessions (example mapping)

Use your RS doc for the exact line ranges. The flow below matches the safest industry approach (move smallest, lowest-risk slices first; keep external API unchanged):

1. **Orchestrator** → thin service that answers “what’s the current run doing?”
2. **Runner** → runs tests/commands in isolation
3. **Planner** → produces/updates plans
4. **Repair** → applies fixes generated by LLMs/agents
5. **LLM Gateway** → one place that calls models/providers
6. **Files/Status** → file IO + status reads
7. **Executor** → kicks off/steers executions

Each one: wire via the flag; keep endpoints and payloads identical; no behavioral changes—just moving rooms in the same house. This mirrors strangler/feature-flag refactors used by AWS/Azure guidance. ([docs.aws.amazon.com][5])

# Copy-paste “Phase Starter” (use this for each phase)

> **ROLE**: You are extracting one microservice from a working monolith **without changing behavior**.
> **NON-NEGOTIABLE RULES**:
>
> 1. Run verification gates **before you touch code** and **after you finish**. Paste full outputs.
> 2. **Do only this phase**. Stop when done.
> 3. Preserve API paths, payloads, status codes, and log messages **exactly**.
> 4. Introduce/obey the flag `SERVICES_SPLIT`:
>      • `0` → monolith path (rollback)
>      • `1` → call the new service over HTTP
> 5. If any gate fails or tests/coverage drop → **STOP** and fix; do not proceed.
>
> **CONTEXT**:
> [Paste the **Phase N** section from **RS_Summary_qodo_original.md** including exact file + line ranges to move]
>
> **TASKS**:
>
> * Create `services/<service-name>/` with minimal prod scaffolding.
> * Move only the listed code (no rewrites, no “improvements”).
> * Add a tiny adapter in the monolith guarded by `SERVICES_SPLIT`.
> * Keep public API 1:1.
>
> **VERIFICATION GATES (paste outputs)**:
>
> ```
> npm run lint && npm run typecheck && npm test && npm run build
> ```
>
> (Include any extra project gates you use, e.g., contracts/SBOM/provenance.)
>
> **REPORT FORMAT**:
>
> * **Before**: gate outputs (all green)
> * **Diff**: files changed/added
> * **After**: gate outputs (all green)
> * **Status**: PASS ✅ / FAIL ❌ with exact error if any

# If you want a ready “Phase 1” prompt now

Replace the context block above with the **Phase 1** excerpt from RS_Summary_qodo_original.md (the exact lines for Orchestrator). That’s it—your assistant can run immediately. If you later prefer an agentic tool that edits files/tests directly from the prompt (with inline diffs, streaming, etc.), both Anthropic’s **Claude Code** and similar IDE-integrated agents support step-wise refactors with test feedback loops—still run **your** gates every step. ([anthropic.com][6])

---

### Why this works (industry signal)

* **Incremental strangler + flags** is the de-risked way big shops migrate monoliths. ([martinfowler.com][1])
* **Keep tests green between tiny moves**—that’s the essence of safe refactoring (branch-by-abstraction discipline). ([martinfowler.com][7])
* **AI assistants** do better with **narrow context + explicit evidence checks** (pre/post test outputs, halt on failure), per current tooling and research. ([anthropic.com][4])

If you want, I can turn the starter into a **one-screen checklist** you can paste at the top of each phase to make your assistant’s job even more turnkey.

[1]: https://martinfowler.com/bliki/StranglerFigApplication.html?utm_source=chatgpt.com "Strangler Fig"
[2]: https://claydowling.com/2017/04/15/branching-by-abstraction.html?utm_source=chatgpt.com "Branching By Abstraction - Clay Dowling"
[3]: https://martinfowler.com/articles/feature-toggles.html?utm_source=chatgpt.com "Feature Toggles (aka Feature Flags)"
[4]: https://www.anthropic.com/engineering/claude-code-best-practices?utm_source=chatgpt.com "Claude Code: Best practices for agentic coding"
[5]: https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/strangler-fig.html?utm_source=chatgpt.com "Strangler fig pattern - AWS Prescriptive Guidance"
[6]: https://www.anthropic.com/news/claude-4?utm_source=chatgpt.com "Introducing Claude 4"
[7]: https://martinfowler.com/bliki/BranchByAbstraction.html?utm_source=chatgpt.com "Branch By Abstraction"
