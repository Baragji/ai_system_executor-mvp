# Phase 19 T0 Delivery Validation — 2025-10-14

## Summary
- ✅ Confirmed the repository includes the promised Trust Spine code updates (status markers, OTel resource metadata, JSONL dual-write, RFC 9457 fixes, graceful shutdown, and helper scripts).
- ⚠️ Found multiple evidence gaps: the published Gate G2 bundle still lacks the SBOM and provenance artifacts, and generated data volume/counts differ materially from the delivery report.
- ⚠️ Provenance generation currently attests only two SBOM files because no compiled artifacts are present; the delivery claim of "71 artifacts" is inaccurate without additional build outputs.
- ⚠️ The CycloneDX SBOM contains 619 components (~1.54 MB), not the 680 components / 1.7 MB quoted in the report.
- ✅ Linting, type-checking, and SBOM/Provenance commands run cleanly, demonstrating the scripts do execute, even though the outputs diverge from reported metrics.

## Claim-by-Claim Assessment

### Repository & Governance Updates
| Claim | Status | Evidence |
| --- | --- | --- |
| CDI infrastructure map now shows ⭐ status for Trust Spine assets | ✅ | `CDI_INFRASTRUCTURE.md` lists problemDetails.ts, otel.ts, events.ts, Trust Spine scripts, and the G2 bundle with ⭐ markers.【F:CDI_INFRASTRUCTURE.md†L223-L247】 |
| OTel bootstrap merges semantic resource metadata | ✅ | `src/telemetry/otel.ts` imports `@opentelemetry/resources` and `@opentelemetry/semantic-conventions`, then merges SERVICE_NAME and SERVICE_VERSION into the NodeSDK resource.【F:src/telemetry/otel.ts†L12-L85】 |
| JSONL dual-write and RFC 9457 helpers implemented behind feature flags | ✅ | `src/telemetry/events.ts` adds ACTION_LOG_JSONL-gated writes, and `src/middleware/problemDetails.ts` corrects occurred_at/reason phrases while remaining feature-flag aware.【F:src/telemetry/events.ts†L1-L107】【F:src/middleware/problemDetails.ts†L1-L154】 |
| Graceful shutdown flushes telemetry | ✅ | `src/server.ts` installs SIGTERM/SIGINT handlers that call `shutdownTelemetry()` before exit.【F:src/server.ts†L2260-L2277】 |
| Helper scripts for SBOM, provenance, and trace generation exist | ✅ | `scripts/generate-cyclonedx.js`, `scripts/generate-provenance.js`, and `scripts/generate-otel-sample.js` are present with detailed logic for their tasks.【F:scripts/generate-cyclonedx.js†L1-L52】【F:scripts/generate-provenance.js†L1-L116】【F:scripts/generate-otel-sample.js†L1-L165】 |
| Package.json lists all eight approved Trust Spine dependencies | ✅ | Dev deps add CycloneDX/Sigstore and runtime deps include the six OTel packages with the stated versions.【F:package.json†L6-L80】 |
| AGENTS.md documents the new validation commands | ✅ | Mandatory checklist now lists `npm run sbom:cyclonedx`, `npm run sbom:all`, and `npm run provenance`.【F:AGENTS.md†L94-L139】 |
| `.gitignore` shields generated evidence artifacts | ✅ | Trust Spine outputs are excluded so local runs do not dirty the repo.【F:.gitignore†L32-L36】 |

### Evidence Bundle & Metrics
| Claim | Status | Evidence |
| --- | --- | --- |
| `.automation/evidence/G2/` contains five artifacts (sbom.cdx.json, provenance.intoto.jsonl, otel_trace_export.json, actions.jsonl, errors_rfc9457.jsonl) | ❌ | Directory currently holds only `otel_trace_export.json`, `actions.jsonl`, and `errors_rfc9457.jsonl`; SBOM and provenance files are absent.【4980e6†L1-L2】 |
| CycloneDX SBOM is 1.7 MB with 680 components | ❌ | Running `npm run sbom:cyclonedx` regenerates the SBOM, but the script reports 619 components and produces a ~1.54 MB file (1 613 445 bytes).【0ca473†L1-L11】【bb50be†L1-L2】 |
| Provenance attests 71 artifacts | ❌ | `npm run provenance` completes successfully yet records only two SBOM subjects; the resulting JSONL shows just those entries.【1378d8†L1-L12】【389ec6†L1-L6】 |
| OTel trace sample uses valid hex IDs with GenAI attributes | ✅ | `otel_trace_export.json` (generated by `scripts/generate-otel-sample.js`) includes 7 resource attributes, two spans, and valid 32-char hex IDs plus GenAI semantic keys.【F:.automation/evidence/G2/otel_trace_export.json†L1-L78】【F:scripts/generate-otel-sample.js†L15-L165】 |
| Gate G2 ledger accurately summarizes evidence results | ❌ | Ledger still asserts 5 artifacts, 680 SBOM components, and 71 provenance artifacts despite the discrepancies above.【F:.automation/GATES_LEDGER.md†L61-L122】 |

### Validation Commands
| Command | Result | Evidence |
| --- | --- | --- |
| `npm run lint` | ✅ Passed (no warnings/errors printed) |【e5b723†L1-L6】 |
| `npm run typecheck` | ✅ Passed |【4e4051†L1-L6】 |
| `npm run sbom:cyclonedx` | ✅ Command succeeds but metrics differ (619 components) |【0ca473†L1-L11】 |
| `npm run provenance` | ✅ Command succeeds but emits only two artifacts |【1378d8†L1-L12】 |

## Impact & Recommendations
1. **Publish Complete Evidence** — Copy `sbom.cdx.json` and `provenance.intoto.jsonl` into `.automation/evidence/G2/` (or adjust the ledger/report) so Gate G2 reviewers can access the artifacts promised in CDI docs.【4980e6†L1-L2】【F:CDI_INFRASTRUCTURE.md†L239-L247】
2. **Correct Reported Metrics** — Update `.automation/GATES_LEDGER.md` (and any downstream reports) to reflect the current SBOM component count and provenance artifact total unless additional build outputs are generated to justify the higher numbers.【F:.automation/GATES_LEDGER.md†L61-L122】【0ca473†L1-L11】【389ec6†L1-L6】
3. **Consider Generating Build Artifacts Before Provenance** — Run `npm run build` (or otherwise populate `dist/`) so the provenance script can hash compiled outputs, aligning the evidence with the “71 artifacts” expectation.【1378d8†L1-L12】
4. **Document Evidence Generation Flow** — Clarify in CDI/ledger docs that the provided `scripts/generate-otel-sample.js` creates a synthetic—but spec-compliant—trace, while `scripts/capture-otel-trace.js` can capture real spans when the collector is running, so auditors know how the JSON sample was produced.【F:scripts/generate-otel-sample.js†L15-L165】【F:scripts/capture-otel-trace.js†L1-L165】

## Appendix: Additional Observations
- `scripts/capture-otel-trace.js` now includes an inline OTLP receiver and random port selection to avoid collisions, providing a path to capture real traces for future audits.【F:scripts/capture-otel-trace.js†L1-L165】
- Gate G2 feature implementations remain fully feature-flagged, supporting the low-risk rollout characterization in the delivery summary.【F:src/telemetry/events.ts†L24-L106】【F:src/middleware/problemDetails.ts†L56-L154】【F:src/telemetry/otel.ts†L42-L113】
